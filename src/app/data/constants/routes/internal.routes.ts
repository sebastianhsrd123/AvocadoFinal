export const ROUTES_PATH = {

  AUTH:{
    DEFAULT: `auth`,
    LOGIN:'login',
  },

  PANEL: {
    DEFAULT: 'app',
    USER: 'user'
  }
};

export const INTERNAL_PATHS = {
  AUTH_DEFAULT: `${ROUTES_PATH.AUTH.DEFAULT}`,
  AUTH_LOGIN: `${ROUTES_PATH.AUTH.LOGIN}`,

  PANEL_DEFAULT: `${ROUTES_PATH.PANEL.DEFAULT}`,
  PANEL_USER: `${ROUTES_PATH.PANEL.USER}`
}

export const INTERNAL_ROUTES = {
  AUTH_LOGIN: `/${INTERNAL_PATHS.AUTH_DEFAULT}/${INTERNAL_PATHS.AUTH_LOGIN}`,
  PANEL_USER: `/${INTERNAL_PATHS.PANEL_DEFAULT}/${INTERNAL_PATHS.PANEL_USER}`
}
